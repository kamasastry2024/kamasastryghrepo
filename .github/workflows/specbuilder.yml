name: 'Terraform_Deploy'
on:
  push:
    branches: [ "main" ]
  pull_request:
jobs:
  terraform:
    name: 'terraform'
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@2
    - name: list files in directory
      run: |
       ls -R ./kamasastryghrepo
