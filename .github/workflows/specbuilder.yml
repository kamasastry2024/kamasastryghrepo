name: 'Terraform_Deploy'
on:
  push:
    branches:
      - main
jobs:
  terraform:
    name: 'terraform'
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: list files in directory
      run: |
       ls -R ./
